# Tiny end-to-end smoke run: PAttn on synthetic data (CPU, 1 epoch).
extends = ["../base.toml", "../datasets/smoke.toml", "../models/PAttn.toml"]

[experiment]
description = "Smoke: PAttn"

[experiment.runtime]
device = "cpu"
num_workers = 0

[task]
seq_len = 96
label_len = 0
pred_len = 12
features = "M"

[training]
epochs = 1
batch_size = 16
loss = "mae"
patience = 1

[model.params]
enc_in = 6
d_model = 32
n_heads = 4
d_ff = 64
patch_len = 16
stride = 8
